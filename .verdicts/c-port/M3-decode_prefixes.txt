C-PORT M3 RUNEQ VERDICT — decode_prefixes (prefix-byte run consumer)
Date: 2026-06-03 · base: origin/main a10543c · branch c-port/m3-decode
Ported leaf: native/i386_decode.c decode_prefixes (C lines 72-95, ~24 LOC)
Hexa impl:   native/i386_decode.hexa  fn decode_prefixes(p, avail) -> (n, prefixes)
C harness:   native/i386_decode_m3_runeq.c (#includes i386_decode.c for the static fn)

METHOD (g5/g63 — real bit-exact C-vs-hexa, no fabrication):
  Sub-corpus P : every byte 0..255 alone, avail=1 (single prefix / stop-at-opcode)
  Sub-corpus P2: every byte 0..255 then 0x90, avail=4 (accumulate then stop)
  Sub-corpus P3: a 9-long run of each of the 11 prefix kinds, avail=9 (n<8 cap)
  Output per row: tab-sep (tag byte n prefixes). 256+256+11 = 523 rows.
  Gate = diff(C stdout, hexa stdout) over the P/P2/P3 rows == empty.

RAW (full M3 corpus, all leaves combined — see batch verdict):
  total C lines  : 1069   (P=256 P2=256 P3=11 O=34 R32=256 R16=256)
  total hexa lines: 1069
  diff           : 0 line(s)
  sha256 C   : 8e6acbb8fddcf1281942594014d35f790e433b0b14b75fa5b69dafa596e8a035
  sha256 hexa: 8e6acbb8fddcf1281942594014d35f790e433b0b14b75fa5b69dafa596e8a035

VERDICT: PORT-EQ (byte-identical)

SAMPLE RAW ROWS (C == hexa, tab-sep tag byte n prefixes):
  P	0	0	0          (0x00 not a prefix → n=0, mask=0)
  P	102	1	1          (0x66 OPSZ → n=1, mask=0x01)
  P	103	1	2          (0x67 ADDRSZ → n=1, mask=0x02)
  P	240	1	16         (0xF0 LOCK → n=1, mask=0x10)

NOTE: hexa `int` is 64-bit; C prefixes_out is uint16_t. All 11 prefix bits
fit in the low 11 bits (max 0x7FF), so OR-accumulation is identical in both;
no row exceeds uint16_t range, so the width difference never manifests.
