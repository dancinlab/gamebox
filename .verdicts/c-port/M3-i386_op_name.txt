C-PORT M3 RUNEQ VERDICT — i386_op_name (op enum → mnemonic string)
Date: 2026-06-03 · base: origin/main a10543c · branch c-port/m3-decode
Ported leaf: native/i386_decode.c i386_op_name (C lines 34-70, ~37 LOC)
Hexa impl:   native/i386_decode.hexa  fn i386_op_name(op) -> str
C harness:   native/i386_decode_m3_runeq.c (#includes i386_decode.c)

METHOD (g5/g63 — real bit-exact C-vs-hexa, no fabrication):
  Sub-corpus O: op enum 0..33 — covers every declared op_t value (0..31)
                PLUS 32, 33 (out of range → C `default` "(unknown)").
  Output per row: tab-sep (tag op mnemonic). 34 rows.
  Gate = diff(C stdout, hexa stdout) over the O rows == empty.
  The hexa port replicates the C switch's collapsed cases (e.g. all four
  MOV variants → "mov", both XOR variants → "xor") and the fall-through
  default exactly.

RAW (full M3 corpus, all leaves combined):
  total C lines  : 1069
  total hexa lines: 1069
  diff           : 0 line(s)
  sha256 C/hexa : 8e6acbb8fddcf1281942594014d35f790e433b0b14b75fa5b69dafa596e8a035

VERDICT: PORT-EQ (byte-identical, all 34 op codes incl. OOB → "(unknown)")

SAMPLE RAW ROWS (C == hexa, tab-sep tag op mnemonic):
  O	1	nop
  O	16	j_cc
  O	31	(prefix-only)
  O	33	(unknown)         (out-of-range → C default branch)

NOTE: enum integer values are taken from i386_decode.h declaration order
(sequential from 0, no explicit values), confirmed against the C harness
which casts the loop int to (i386_op_t). RUNEQ over the full 0..31 range
+ OOB is what pins the enum-ordinal assumption.
